%ul
  - if current_user
    %li= t("welcome", :name => current_user.greeting)
    %li= link_to t("logout"), destroy_user_session_url

  - else
    %li= link_to t("sign_in"), new_user_session_url
    %li= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook)
        